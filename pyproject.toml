[project]
name = "uavvisuallocalization"
version = "0.1.0"
description = "No GPS Localization"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "opencv-python",
    "faiss-cpu",
    "omegaconf",
]

[dependency-groups]
dev = [
    "torch",
    "matplotlib",
    "rasterio",
    "ipykernel",
    "jupyter",
]

pi = [
    "onnxruntime",
]

[tool.uv]
package = false